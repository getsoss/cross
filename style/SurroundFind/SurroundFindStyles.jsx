import { StyleSheet } from "react-native";

export default SurroundFindStyles = StyleSheet.create({
  scrollView: {
    flex: 1,
    backgroundColor: "white",
  },
  webViewContainer: {
    flex: 15,
  },
});
